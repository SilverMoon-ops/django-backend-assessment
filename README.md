# Django Backend Assessment

Backend API service built with Django and Django REST Framework.

## Setup Instructions
1. Clone the repository: git clone https://github.com/SilverMoon-ops/django-backend-assessment.git
2. Install dependencies: pip install -r requirements.txt
3. Run server: python manage.py runserver
